<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jason.mapper.SampleMapper">

    <insert id="DML">
        ${sql}
    </insert>

    <select id="getSingleColumnString" resultType="String">
        ${sql}
    </select>

    <select id="selectSingleColumnStringList" resultType="String">
        ${sql}
    </select>

    <select id="selectObject" resultType="java.util.LinkedHashMap">
        ${sql}
    </select>
</mapper>